import { Http } from './http.init'

export class BaseService {
  /**
   * ------------------------------
   * @HELPERS
   * ------------------------------
   */

  static request () {
    return new Http(status)
  }
}
